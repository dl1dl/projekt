@model Recipe

<div>
    <h2>@Model.Name</h2>    
    <p>@Model.Body</p>

    <form asp-controller="Home" asp-action="Details">
        <input type="hidden" asp-for="RecipeID" />
        <button type="submit" class="btn btn-primary">Zobacz więcej</button>
    </form>
</div>
<br />
<hr />