@model IEnumerable<Category>
@{
    ViewData["Title"] = "Index";
}

<h2>Wybierz kategorię: </h2>

@foreach (var c in Model)
{
    <a asp-action="AllRecipes" asp-route-id="@c.CategoryID" class="btn btn-info">@c.Name</a>
}