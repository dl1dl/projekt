@model Category

@{
    ViewData["Title"] = "All";
}

<h2>Przepisy w kategorii @Model.Name</h2>

<ul>
@foreach (var r in Model.Recipes)
{
    <li><a asp-controller="Recipe" asp-action="Details" asp-route-id="@r.RecipeID">@r.Name</a></li>
}
</ul>